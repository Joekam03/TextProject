# Table Exports

Run `FinalProject.ipynb` from top to bottom. The final `Table Exports` cell writes the generated project tables into this folder as CSV files.

The markdown links in the final notebook point here so the rendered notebook and PDF can refer readers to the same table artifacts.
